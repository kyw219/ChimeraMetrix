{
  "buildCommand": "cd fronted && npm ci && npm run build",
  "outputDirectory": "fronted/dist",
  "installCommand": "npm install",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 60,
      "includeFiles": "backtest-data.csv"
    }
  }
}
